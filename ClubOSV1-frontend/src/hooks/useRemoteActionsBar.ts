// RemoteActionsBar has been removed. This hook remains as a stub
// so that existing consumers don't break.
export function useRemoteActionsBar() {
  return {
    isVisible: false,
    height: 0,
    className: ''
  };
}
